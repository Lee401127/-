#pragma once
#include "MysqlUtil.h"
#include<list>
#include "CUser.h"
using namespace std;
class CUserModel
{
private:



public:
	CUserModel();
	~CUserModel();
	//????????????	
	list<CUser*> selectByPhone(char phone[12]);
	//???????
	bool insertUser(CUser u);
	//????????????????(????????)
	bool  updateStatusByPhone(char phone[12], int status);
	//???????????????????
	list<CUser*> selectByphoneAndPwd(string phone, string password);
	// DB identity_id: 1 elder, 2 caregiver, 0 family; or -1 on failure
	int getIdentityIfPasswordOk(const string& phone, const string& password);

};

